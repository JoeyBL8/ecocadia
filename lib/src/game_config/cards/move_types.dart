

import 'package:new_card_game/src/game_config/creature.dart';

class MoveTypes {
  static const MoveType quick = MoveType(name: 'quick');
  static const MoveType ignition = MoveType(name: 'ignition');
  static const MoveType trigger = MoveType(name: 'trigger');
  static const MoveType continuous = MoveType(name: 'continuous');
  static const MoveType attack = MoveType(name: 'attack');
  static const MoveType defensive = MoveType(name: 'defensive');
}
